/**
 * Production entry point wrapper for shared hosting environments (such as Hostinger, cPanel, etc.)
 * that automatically search for and execute a root level `server.js` or `app.js` file.
 */
import "./dist/server.cjs";
